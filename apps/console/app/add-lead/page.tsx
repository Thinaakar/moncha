'use client';import {useState} from 'react';export default function AddLead(){const [msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const obj=Object.fromEntries(f.entries());const res=await fetch('/api/v1/leads',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(obj)});const data=await res.json();setMsg(res.ok?`Created ${data.company?.name}`:data.error)}return <main><h1>Add lead</h1><div className="card"><form onSubmit={submit}>{['tenantId','name','domain','country','city','phone','address'].map(x=><div key={x} style={{marginBottom:10}}><input name={x} placeholder={x} required={['tenantId','name'].includes(x)}/></div>)}<button>Create</button></form>{msg&&<p>{msg}</p>}</div></main>}
