'use client';import {useState} from 'react';export default function Discover(){const [msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const res=await fetch('/api/v1/source-imports',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({tenantId:String(f.get('tenantId')),country:String(f.get('country')),city:String(f.get('city')),keyword:String(f.get('keyword'))})});const data=await res.json();setMsg(res.ok?`Job created: ${data.id}`:data.error)}return <main><h1>Discover</h1><div className="card"><form onSubmit={submit}><input name="tenantId" placeholder="Tenant ID" required/><br/><br/><input name="country" placeholder="Country" required/><br/><br/><input name="city" placeholder="City" required/><br/><br/><input name="keyword" placeholder="Keyword / industry" required/><br/><br/><button>Start auto discovery</button></form>{msg&&<p>{msg}</p>}</div></main>}
