import {NextResponse} from 'next/server';import {discoverSchema} from '@moncha/contracts';import {prisma,PrismaJobRunRepository} from '@moncha/db';import {tasks} from '@trigger.dev/sdk/v3';
export async function POST(req:Request){try{const input=discoverSchema.parse(await req.json());const job=await new PrismaJobRunRepository(prisma).create(input.tenantId,'places_discovery',input);await tasks.trigger('places-discovery',{...input,jobId:job.id});return NextResponse.json(job,{status:202});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Invalid request'},{status:400})}}
