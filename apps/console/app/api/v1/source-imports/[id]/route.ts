import {NextResponse} from 'next/server';import {prisma,PrismaJobRunRepository} from '@moncha/db';export async function GET(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const tenantId=new URL(req.url).searchParams.get('tenantId');if(!tenantId)return NextResponse.json({error:'tenantId required'},{status:400});const job=await new PrismaJobRunRepository(prisma).get(tenantId,id);return job?NextResponse.json(job):NextResponse.json({error:'Not found'},{status:404})}
