import {NextResponse} from 'next/server';import {prisma,PrismaLeadRepository} from '@moncha/db';export async function GET(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const tenantId=new URL(req.url).searchParams.get('tenantId');if(!tenantId)return NextResponse.json({error:'tenantId required'},{status:400});const lead=await new PrismaLeadRepository(prisma).get(tenantId,id);return lead?NextResponse.json(lead):NextResponse.json({error:'Not found'},{status:404})}
